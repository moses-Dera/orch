{
  "name": "orch-review-action",
  "version": "1.0.0",
  "description": "GitHub Action for automated code review against org constraints. Governance over all AI, regardless of source.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/orch-dev/review-action"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "minimatch": "^9.0.4"
  },
  "devDependencies": {
    "@types/minimatch": "^5.1.2",
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.0"
  }
}
