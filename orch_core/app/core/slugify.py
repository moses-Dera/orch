import re
import secrets


def _slugify(text: str) -> str:
    text = text.lower().strip()
    text = re.sub(r"[^\w\s-]", "", text)
    text = re.sub(r"[\s_-]+", "-", text)
    text = re.sub(r"^-+|-+$", "", text)
    suffix = secrets.token_hex(3)
    return f"{text[:56]}-{suffix}"
