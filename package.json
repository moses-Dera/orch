{
  "name": "orch",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd packages/core dev\" \"bun run --cwd apps/dashboard dev\" \"bun run --cwd apps/web dev\" \"bun run --cwd apps/docs dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
