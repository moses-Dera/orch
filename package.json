{
  "name": "orch",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd packages/core dev\" \"bun run --cwd apps/dashboard dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "puppeteer": "^25.7.0"
  }
}
